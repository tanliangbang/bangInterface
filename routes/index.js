var express = require('express');
var router = express.Router();
var mysqlMoveMongodb = require('../util/mysqlMoveMongodb');

/* GET home page. */
router.get('/', function(req, res, next) {
    //mysqlMoveMongodb.move()
});

module.exports = router;
